# Copie este arquivo para .env e ajuste os valores conforme seu ambiente.
# Para rodar com Docker, o host do banco deve ser "db" (nome do serviço no docker-compose).
DATABASE_URL=mysql+pymysql://bank_user:bank123@db:3306/bank_system_db
